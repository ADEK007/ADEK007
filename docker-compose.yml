version: "3.9"
services:
  web:
    image: php:8.2-apache
    container_name: nasa-explorer-web
    ports:
      - "8080:80"
    volumes:
      - ./public:/var/www/html:rw
      - ./data:/var/www/data:rw
    environment:
      - APACHE_LOG_DIR=/var/log/apache2
    restart: unless-stopped

